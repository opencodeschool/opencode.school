// Copyright (c) 2026 Cloudflare, Inc.
// Licensed under the Apache 2.0 license found in the LICENSE file or at:
//     https://opensource.org/licenses/Apache-2.0
---
import "../styles/main.css";
import { getCollection } from "astro:content";

interface Props {
  title?: string;
  description?: string;
}

const { title, description = "Learn to use OpenCode, the free and open-source AI coding agent." } = Astro.props;
const pageTitle = title ? `${title} | OpenCode School` : "OpenCode School";

const lessons = (await getCollection("lessons")).sort((a, b) => a.data.order - b.data.order);
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{pageTitle}</title>
    <script is:inline>
      window.__schoolThemePalettes = {
        blue: {
          solid: "#2563eb",
          solidHover: "#1d4ed8",
          link: "#2563eb",
          linkDark: "#60a5fa",
          softBg: "#eff6ff",
          softBorder: "#bfdbfe",
          softText: "#1d4ed8",
          softBgDark: "rgba(37, 99, 235, 0.18)",
          softBorderDark: "rgba(96, 165, 250, 0.35)",
          softTextDark: "#93c5fd",
          cardText: "#dbeafe",
        },
        indigo: {
          solid: "#4f46e5",
          solidHover: "#4338ca",
          link: "#4338ca",
          linkDark: "#a5b4fc",
          softBg: "#eef2ff",
          softBorder: "#c7d2fe",
          softText: "#4338ca",
          softBgDark: "rgba(79, 70, 229, 0.18)",
          softBorderDark: "rgba(165, 180, 252, 0.35)",
          softTextDark: "#c7d2fe",
          cardText: "#e0e7ff",
        },
        violet: {
          solid: "#7c3aed",
          solidHover: "#6d28d9",
          link: "#6d28d9",
          linkDark: "#c4b5fd",
          softBg: "#f5f3ff",
          softBorder: "#ddd6fe",
          softText: "#6d28d9",
          softBgDark: "rgba(124, 58, 237, 0.18)",
          softBorderDark: "rgba(196, 181, 253, 0.35)",
          softTextDark: "#ddd6fe",
          cardText: "#ede9fe",
        },
        purple: {
          solid: "#9333ea",
          solidHover: "#7e22ce",
          link: "#7e22ce",
          linkDark: "#d8b4fe",
          softBg: "#faf5ff",
          softBorder: "#e9d5ff",
          softText: "#7e22ce",
          softBgDark: "rgba(147, 51, 234, 0.18)",
          softBorderDark: "rgba(216, 180, 254, 0.35)",
          softTextDark: "#e9d5ff",
          cardText: "#f3e8ff",
        },
        fuchsia: {
          solid: "#c026d3",
          solidHover: "#a21caf",
          link: "#a21caf",
          linkDark: "#f0abfc",
          softBg: "#fdf4ff",
          softBorder: "#f5d0fe",
          softText: "#a21caf",
          softBgDark: "rgba(192, 38, 211, 0.18)",
          softBorderDark: "rgba(240, 171, 252, 0.35)",
          softTextDark: "#f5d0fe",
          cardText: "#fae8ff",
        },
        pink: {
          solid: "#db2777",
          solidHover: "#be185d",
          link: "#be185d",
          linkDark: "#f472b6",
          softBg: "#fdf2f8",
          softBorder: "#fbcfe8",
          softText: "#be185d",
          softBgDark: "rgba(219, 39, 119, 0.18)",
          softBorderDark: "rgba(244, 114, 182, 0.35)",
          softTextDark: "#f9a8d4",
          cardText: "#fce7f3",
        },
        rose: {
          solid: "#e11d48",
          solidHover: "#be123c",
          link: "#be123c",
          linkDark: "#fda4af",
          softBg: "#fff1f2",
          softBorder: "#fecdd3",
          softText: "#be123c",
          softBgDark: "rgba(225, 29, 72, 0.18)",
          softBorderDark: "rgba(251, 113, 133, 0.35)",
          softTextDark: "#fda4af",
          cardText: "#ffe4e6",
        },
        red: {
          solid: "#dc2626",
          solidHover: "#b91c1c",
          link: "#b91c1c",
          linkDark: "#fca5a5",
          softBg: "#fef2f2",
          softBorder: "#fecaca",
          softText: "#b91c1c",
          softBgDark: "rgba(220, 38, 38, 0.18)",
          softBorderDark: "rgba(248, 113, 113, 0.35)",
          softTextDark: "#fca5a5",
          cardText: "#fee2e2",
        },
        orange: {
          solid: "#ea580c",
          solidHover: "#c2410c",
          link: "#c2410c",
          linkDark: "#fdba74",
          softBg: "#fff7ed",
          softBorder: "#fed7aa",
          softText: "#c2410c",
          softBgDark: "rgba(234, 88, 12, 0.18)",
          softBorderDark: "rgba(251, 146, 60, 0.35)",
          softTextDark: "#fdba74",
          cardText: "#ffedd5",
        },
        amber: {
          solid: "#b45309",
          solidHover: "#92400e",
          link: "#92400e",
          linkDark: "#fcd34d",
          softBg: "#fffbeb",
          softBorder: "#fde68a",
          softText: "#92400e",
          softBgDark: "rgba(180, 83, 9, 0.18)",
          softBorderDark: "rgba(251, 191, 36, 0.35)",
          softTextDark: "#fcd34d",
          cardText: "#fef3c7",
        },
        yellow: {
          solid: "#ca8a04",
          solidHover: "#a16207",
          link: "#a16207",
          linkDark: "#facc15",
          softBg: "#fefce8",
          softBorder: "#fef08a",
          softText: "#a16207",
          softBgDark: "rgba(202, 138, 4, 0.18)",
          softBorderDark: "rgba(250, 204, 21, 0.35)",
          softTextDark: "#fde047",
          cardText: "#fef9c3",
        },
        lime: {
          solid: "#4d7c0f",
          solidHover: "#3f6212",
          link: "#4d7c0f",
          linkDark: "#bef264",
          softBg: "#f7fee7",
          softBorder: "#d9f99d",
          softText: "#4d7c0f",
          softBgDark: "rgba(77, 124, 15, 0.18)",
          softBorderDark: "rgba(132, 204, 22, 0.35)",
          softTextDark: "#bef264",
          cardText: "#ecfccb",
        },
        green: {
          solid: "#15803d",
          solidHover: "#166534",
          link: "#166534",
          linkDark: "#86efac",
          softBg: "#f0fdf4",
          softBorder: "#bbf7d0",
          softText: "#166534",
          softBgDark: "rgba(21, 128, 61, 0.18)",
          softBorderDark: "rgba(74, 222, 128, 0.35)",
          softTextDark: "#86efac",
          cardText: "#dcfce7",
        },
        emerald: {
          solid: "#059669",
          solidHover: "#047857",
          link: "#047857",
          linkDark: "#6ee7b7",
          softBg: "#ecfdf5",
          softBorder: "#a7f3d0",
          softText: "#047857",
          softBgDark: "rgba(5, 150, 105, 0.18)",
          softBorderDark: "rgba(110, 231, 183, 0.35)",
          softTextDark: "#a7f3d0",
          cardText: "#d1fae5",
        },
        teal: {
          solid: "#0f766e",
          solidHover: "#115e59",
          link: "#0f766e",
          linkDark: "#5eead4",
          softBg: "#f0fdfa",
          softBorder: "#99f6e4",
          softText: "#115e59",
          softBgDark: "rgba(15, 118, 110, 0.2)",
          softBorderDark: "rgba(94, 234, 212, 0.35)",
          softTextDark: "#99f6e4",
          cardText: "#ccfbf1",
        },
        cyan: {
          solid: "#0e7490",
          solidHover: "#155e75",
          link: "#155e75",
          linkDark: "#67e8f9",
          softBg: "#ecfeff",
          softBorder: "#a5f3fc",
          softText: "#155e75",
          softBgDark: "rgba(14, 116, 144, 0.2)",
          softBorderDark: "rgba(103, 232, 249, 0.35)",
          softTextDark: "#a5f3fc",
          cardText: "#cffafe",
        },
        sky: {
          solid: "#0284c7",
          solidHover: "#0369a1",
          link: "#0369a1",
          linkDark: "#7dd3fc",
          softBg: "#f0f9ff",
          softBorder: "#bae6fd",
          softText: "#0369a1",
          softBgDark: "rgba(2, 132, 199, 0.18)",
          softBorderDark: "rgba(56, 189, 248, 0.35)",
          softTextDark: "#bae6fd",
          cardText: "#e0f2fe",
        },
        slate: {
          solid: "#475569",
          solidHover: "#334155",
          link: "#334155",
          linkDark: "#cbd5e1",
          softBg: "#f8fafc",
          softBorder: "#cbd5e1",
          softText: "#334155",
          softBgDark: "rgba(71, 85, 105, 0.3)",
          softBorderDark: "rgba(148, 163, 184, 0.35)",
          softTextDark: "#cbd5e1",
          cardText: "#e2e8f0",
        },
      };

      window.__applySchoolTheme = function(themeName) {
        var palette = window.__schoolThemePalettes[themeName] || window.__schoolThemePalettes.blue;
        var root = document.documentElement;
        root.style.setProperty("--theme-solid", palette.solid);
        root.style.setProperty("--theme-solid-hover", palette.solidHover);
        root.style.setProperty("--theme-link", palette.link);
        root.style.setProperty("--theme-link-dark", palette.linkDark);
        root.style.setProperty("--theme-soft-bg", palette.softBg);
        root.style.setProperty("--theme-soft-border", palette.softBorder);
        root.style.setProperty("--theme-soft-text", palette.softText);
        root.style.setProperty("--theme-soft-bg-dark", palette.softBgDark);
        root.style.setProperty("--theme-soft-border-dark", palette.softBorderDark);
        root.style.setProperty("--theme-soft-text-dark", palette.softTextDark);
        root.style.setProperty("--theme-card-text", palette.cardText);
        root.dataset.themeColor = window.__schoolThemePalettes[themeName] ? themeName : "blue";
      };

      try {
        window.__applySchoolTheme(localStorage.getItem("themeColor") || "blue");
      } catch {
        window.__applySchoolTheme("blue");
      }
    </script>
    <script is:inline>
      if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
        document.documentElement.classList.add("dark");
      }
      window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change", (e) => {
        document.documentElement.classList.toggle("dark", e.matches);
      });
    </script>
    <script is:inline>
      // Progress tracking API — defined in <head> so page-level scripts can use it
      window.school = {
        getStudentId() {
          return localStorage.getItem("studentId");
        },

        setStudentId(id) {
          localStorage.setItem("studentId", id);
        },

        getThemeColor() {
          return localStorage.getItem("themeColor") || "blue";
        },

        getThemePalettes() {
          return window.__schoolThemePalettes;
        },

        setThemeColor(themeName) {
          const palettes = this.getThemePalettes();
          const nextTheme = palettes[themeName] ? themeName : "blue";
          localStorage.setItem("themeColor", nextTheme);
          window.__applySchoolTheme(nextTheme);
          return nextTheme;
        },

        getCachedProgress() {
          try {
            return JSON.parse(localStorage.getItem("progress") || "null");
          } catch { return null; }
        },

        setCachedProgress(progress) {
          localStorage.setItem("progress", JSON.stringify(progress));
        },

        isLessonComplete(slug) {
          const progress = this.getCachedProgress();
          return progress?.completedLessons?.some((l) => (typeof l === "string" ? l : l.slug) === slug) || false;
        },

        getLessonCompletion(slug) {
          const progress = this.getCachedProgress();
          const entry = progress?.completedLessons?.find((l) => (typeof l === "string" ? l : l.slug) === slug);
          if (!entry) return null;
          return typeof entry === "string" ? { slug: entry, completedAt: null, source: "browser" } : entry;
        },

        getVisitedLessons() {
          try {
            return JSON.parse(localStorage.getItem("visitedLessons") || "[]");
          } catch { return []; }
        },

        isLessonVisited(slug) {
          return this.getVisitedLessons().includes(slug);
        },

        markLessonVisited(slug) {
          const visited = this.getVisitedLessons();
          if (!visited.includes(slug)) {
            visited.push(slug);
            localStorage.setItem("visitedLessons", JSON.stringify(visited));
          }
        },

        async enroll() {
          const res = await fetch("/api/enroll", { method: "POST" });
          if (!res.ok) throw new Error("Enrollment failed");
          const data = await res.json();
          this.setStudentId(data.studentId);
          this.setCachedProgress(data.progress);
          return data;
        },

        async fetchProgress() {
          const studentId = this.getStudentId();
          if (!studentId) return null;
          const res = await fetch(`/api/progress/${studentId}`);
          if (!res.ok) return null;
          const progress = await res.json();
          this.setCachedProgress(progress);
          return progress;
        },

        async markComplete(lessonSlug) {
          const studentId = this.getStudentId();
          if (!studentId) return null;
          const res = await fetch(`/api/progress/${studentId}`, {
            method: "PUT",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ lessonSlug, source: "browser" }),
          });
          if (!res.ok) return null;
          const progress = await res.json();
          this.setCachedProgress(progress);
          return progress;
        },

        updateAllCheckmarks() {
          let nextIncompleteSet = false;
          document.querySelectorAll("[data-lesson-slug]").forEach((el) => {
            const slug = el.getAttribute("data-lesson-slug");
            const isStub = el.getAttribute("data-lesson-stub") === "true";
            const rowEl = el.querySelector(".lesson-row");
            const linkEl = el.querySelector(".lesson-link");
            const ctaEl = el.querySelector(".lesson-cta");
            const statusEl = el.querySelector(".lesson-status");
            const checkEl = el.querySelector(".lesson-check");
            const dotEl = el.querySelector(".lesson-status-dot");
            const complete = this.isLessonComplete(slug);
            const isNext = !complete && !isStub && !nextIncompleteSet;
            if (isNext) nextIncompleteSet = true;

            if (rowEl) {
              rowEl.classList.toggle("lesson-row--next", isNext);
            }
            if (linkEl) {
              linkEl.classList.toggle("sm:flex-1", isNext);
            }
            if (ctaEl) {
              ctaEl.classList.toggle("hidden", !isNext);
              ctaEl.classList.toggle("inline-block", isNext);
              if (isNext) ctaEl.textContent = this.isLessonVisited(slug) ? "Continue" : "Start";
            }
            if (statusEl) {
              statusEl.classList.toggle("lesson-status--complete", complete);
            }
            if (checkEl) {
              checkEl.classList.toggle("hidden", !complete);
              checkEl.classList.toggle("flex", complete);
            }
            if (dotEl) dotEl.classList.toggle("hidden", complete);
          });
          // Update sidebar completion circles
          document.querySelectorAll("[data-sidebar-slug]").forEach((el) => {
            const slug = el.getAttribute("data-sidebar-slug");
            const circle = el.querySelector(".sidebar-check");
            if (circle) {
              const complete = this.isLessonComplete(slug);
              circle.textContent = complete ? "\u2713" : "";
              circle.classList.toggle("hidden", !complete);
              circle.classList.toggle("flex", complete);
            }
          });
        },

        updateEnrolledIndicator() {
          const id = this.getStudentId();
          const el = document.getElementById("enrolled-indicator");
          if (el && id) {
            el.querySelector("[data-student-id]").textContent = id;
            el.classList.remove("hidden");
          }
        },
      };
    </script>
  </head>
  <body class="min-h-screen">
    <div class="lg:flex">
      <!-- Mobile header -->
      <header class="lg:hidden flex items-center justify-between p-4 border-b border-gray-200 dark:border-stone-800">
        <a href="/" class="font-mono font-bold text-lg">OpenCode School</a>
        <button
          id="menu-toggle"
          class="p-2 rounded-md hover:bg-gray-100 dark:hover:bg-stone-800"
          aria-label="Toggle navigation"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </header>

      <!-- Sidebar -->
      <nav
        id="sidebar"
        class="hidden lg:block w-72 shrink-0 border-r border-gray-200 dark:border-stone-800 h-screen sticky top-0 overflow-y-auto p-6"
      >
        <a href="/" class="block font-mono font-bold text-xl mb-6 px-3">OpenCode School</a>
        <ol class="space-y-1">
          {lessons.map((lesson) => {
            const href = `/lessons/${lesson.data.slug}`;
            const isActive = currentPath === href || currentPath === `${href}/`;
            const isStub = lesson.body?.trim() === "Coming soon.";
            return (
              <li data-sidebar-slug={lesson.data.slug}>
                <a
                  href={href}
                  class:list={[
                    "flex items-center justify-between px-3 py-1.5 rounded-md text-sm transition-colors",
                    isActive
                      ? "theme-active-nav font-medium"
                      : "text-gray-600 hover:text-gray-900 hover:bg-gray-50 dark:text-stone-400 dark:hover:text-stone-200 dark:hover:bg-stone-800",
                    isStub && "opacity-50",
                  ]}
                >
                  <span>
                    <span class="text-gray-400 dark:text-stone-600 mr-1.5">{String(lesson.data.order).padStart(2, "0")}.</span>
                    {lesson.data.title}
                  </span>
                  <span class="sidebar-check theme-solid-bg w-4 h-4 rounded-full text-white text-[10px] font-bold items-center justify-center shrink-0 ml-2 hidden"></span>
                </a>
              </li>
            );
          })}
        </ol>
        <div class="mt-6 pt-4 border-t border-gray-200 dark:border-stone-800">
          <a
            href="/glossary"
            class:list={[
              "block px-3 py-1.5 rounded-md text-sm transition-colors",
              currentPath === "/glossary" || currentPath === "/glossary/"
                ? "theme-active-nav font-medium"
                : "text-gray-600 hover:text-gray-900 hover:bg-gray-50 dark:text-stone-400 dark:hover:text-stone-200 dark:hover:bg-stone-800",
            ]}
          >
            Glossary
          </a>
          <a
            href="/troubleshooting"
            class:list={[
              "block px-3 py-1.5 rounded-md text-sm transition-colors",
              currentPath === "/troubleshooting" || currentPath === "/troubleshooting/"
                ? "theme-active-nav font-medium"
                : "text-gray-600 hover:text-gray-900 hover:bg-gray-50 dark:text-stone-400 dark:hover:text-stone-200 dark:hover:bg-stone-800",
            ]}
          >
            Troubleshooting
          </a>
        </div>
      </nav>

      <!-- Enrolled indicator (fixed top-right on desktop) -->
      <div id="enrolled-indicator" class="hidden max-sm:!hidden fixed top-6 right-6 z-40">
        <span class="text-xs font-mono text-gray-400 dark:text-stone-500">Enrolled as <span data-student-id class="text-gray-500 dark:text-stone-400"></span></span>
      </div>

      <!-- Main content -->
      <main class="flex-1 min-w-0 p-6 lg:p-12 max-w-3xl">
        <slot />
      </main>
    </div>

    <!-- Footer -->
    <footer class="border-t border-gray-200 dark:border-stone-800 py-8 px-6 lg:px-12 flex justify-center items-center gap-10">
      <!-- Created and sponsored by Cloudflare -->
      <div class="relative group">
        <a href="https://developers.cloudflare.com/directory/" target="_blank" rel="noopener noreferrer" class="block text-gray-400 dark:text-stone-600 hover:text-orange-400 dark:hover:text-orange-400 transition-colors" aria-label="Created and sponsored by Cloudflare">
          <svg class="w-8 h-8" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M16.5088 16.8447c.1475-.5068.0908-.9707-.1553-1.3154-.2246-.3164-.6045-.499-1.0615-.5205l-8.6592-.1123a.1559.1559 0 0 1-.1333-.0713c-.0283-.042-.0351-.0986-.021-.1553.0278-.084.1123-.1484.2036-.1562l8.7359-.1123c1.0351-.0489 2.1601-.8868 2.5537-1.9136l.499-1.3013c.0215-.0561.0293-.1128.0147-.168-.5625-2.5463-2.835-4.4453-5.5499-4.4453-2.5039 0-4.6284 1.6177-5.3876 3.8614-.4927-.3658-1.1187-.5625-1.794-.499-1.2026.119-2.1665 1.083-2.2861 2.2856-.0283.31-.0069.6128.0635.894C1.5683 13.171 0 14.7754 0 16.752c0 .1748.0142.3515.0352.5273.0141.083.0844.1475.1689.1475h15.9814c.0909 0 .1758-.0645.2032-.1553l.12-.4268zm2.7568-5.5634c-.0771 0-.1611 0-.2383.0112-.0566 0-.1054.0415-.127.0976l-.3378 1.1744c-.1475.5068-.0918.9707.1543 1.3164.2256.3164.6055.498 1.0625.5195l1.8437.1133c.0557 0 .1055.0263.1329.0703.0283.043.0351.1074.0214.1562-.0283.084-.1132.1485-.204.1553l-1.921.1123c-1.041.0488-2.1582.8867-2.5527 1.914l-.1406.3585c-.0283.0713.0215.1416.0986.1416h6.5977c.0771 0 .1474-.0489.169-.126.1122-.4082.1757-.837.1757-1.2803 0-2.6025-2.125-4.727-4.7344-4.727"/>
          </svg>
        </a>
        <span class="pointer-events-none absolute bottom-full left-1/2 -translate-x-1/2 mb-2 whitespace-nowrap rounded px-2 py-1 text-xs bg-gray-800 dark:bg-stone-700 text-white opacity-0 group-hover:opacity-100 transition-opacity">Created and sponsored by Cloudflare</span>
      </div>

      <!-- Built with OpenCode -->
      <div class="relative group">
        <a href="https://opencode.ai" target="_blank" rel="noopener noreferrer" class="block text-gray-400 dark:text-stone-600 hover:text-gray-600 dark:hover:text-stone-400 transition-colors" aria-label="Built with OpenCode">
          <!-- light mode icon -->
          <svg class="dark:hidden w-8 h-8" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M10 17H4V10H10V17Z" fill="#CFCECD"/>
            <path d="M10 7H4V17H10V7ZM13 20H1V4H13V20Z" fill="#656363"/>
          </svg>
          <!-- dark mode icon -->
          <svg class="hidden dark:block w-8 h-8" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M10 17H4V10H10V17Z" fill="#4B4646"/>
            <path d="M10 7H4V17H10V7ZM13 20H1V4H13V20Z" fill="#B7B1B1"/>
          </svg>
        </a>
        <span class="pointer-events-none absolute bottom-full left-1/2 -translate-x-1/2 mb-2 whitespace-nowrap rounded px-2 py-1 text-xs bg-gray-800 dark:bg-stone-700 text-white opacity-0 group-hover:opacity-100 transition-opacity">Built with OpenCode</span>
      </div>

      <!-- Open source on GitHub -->
      <div class="relative group">
        <a href="https://github.com/opencodeschool/opencode.school" target="_blank" rel="noopener noreferrer" class="block text-gray-400 dark:text-stone-600 hover:text-gray-600 dark:hover:text-stone-400 transition-colors" aria-label="Open source on GitHub">
          <svg class="w-8 h-8" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M12 2C6.477 2 2 6.477 2 12c0 4.418 2.865 8.166 6.839 9.489.5.092.682-.217.682-.482 0-.237-.009-.868-.013-1.703-2.782.604-3.369-1.342-3.369-1.342-.454-1.154-1.11-1.462-1.11-1.462-.908-.62.069-.608.069-.608 1.003.07 1.531 1.03 1.531 1.03.892 1.529 2.341 1.087 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.11-4.555-4.943 0-1.091.39-1.984 1.029-2.683-.103-.253-.446-1.27.098-2.647 0 0 .84-.269 2.75 1.025A9.578 9.578 0 0 1 12 6.836a9.59 9.59 0 0 1 2.504.337c1.909-1.294 2.747-1.025 2.747-1.025.546 1.377.203 2.394.1 2.647.64.699 1.028 1.592 1.028 2.683 0 3.842-2.339 4.687-4.566 4.935.359.309.678.919.678 1.852 0 1.336-.012 2.415-.012 2.743 0 .267.18.578.688.48C19.138 20.163 22 16.418 22 12c0-5.523-4.477-10-10-10z"/>
          </svg>
        </a>
        <span class="pointer-events-none absolute bottom-full left-1/2 -translate-x-1/2 mb-2 whitespace-nowrap rounded px-2 py-1 text-xs bg-gray-800 dark:bg-stone-700 text-white opacity-0 group-hover:opacity-100 transition-opacity">Open source on GitHub</span>
      </div>

      <!-- OpenCode support community (Discord) -->
      <div class="relative group">
        <a href="https://opencode.ai/discord" target="_blank" rel="noopener noreferrer" class="block text-gray-400 dark:text-stone-600 hover:text-gray-600 dark:hover:text-stone-400 transition-colors" aria-label="OpenCode support community on Discord">
          <svg class="w-8 h-8" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M20.317 4.37a19.791 19.791 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057a.082.082 0 0 0 .031.057 19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.09 14.09 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128 10.2 10.2 0 0 0 .372-.292.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 0 1 .078.01c.12.098.246.198.373.292a.077.077 0 0 1-.006.127 12.299 12.299 0 0 1-1.873.892.077.077 0 0 0-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.839 19.839 0 0 0 6.002-3.03.077.077 0 0 0 .032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.03zM8.02 15.33c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.956-2.419 2.157-2.419 1.21 0 2.176 1.096 2.157 2.42 0 1.333-.956 2.418-2.157 2.418zm7.975 0c-1.183 0-2.157-1.085-2.157-2.419 0-1.333.955-2.419 2.157-2.419 1.21 0 2.176 1.096 2.157 2.42 0 1.333-.946 2.418-2.157 2.418z"/>
          </svg>
        </a>
        <span class="pointer-events-none absolute bottom-full left-1/2 -translate-x-1/2 mb-2 whitespace-nowrap rounded px-2 py-1 text-xs bg-gray-800 dark:bg-stone-700 text-white opacity-0 group-hover:opacity-100 transition-opacity">OpenCode support community</span>
      </div>
    </footer>

    <script is:inline>
      // Mobile menu toggle
      const toggle = document.getElementById("menu-toggle");
      const sidebar = document.getElementById("sidebar");
      if (toggle && sidebar) {
        toggle.addEventListener("click", () => {
          sidebar.classList.toggle("hidden");
          if (!sidebar.classList.contains("hidden")) {
            sidebar.classList.add("fixed", "inset-0", "z-50", "bg-white", "dark:bg-stone-900");
            sidebar.classList.remove("lg:block");
          } else {
            sidebar.classList.remove("fixed", "inset-0", "z-50", "bg-white", "dark:bg-stone-900");
            sidebar.classList.add("lg:block");
          }
        });
      }

      // Handle ?sid= param: save student ID from URL and clean up
      (function() {
        const params = new URLSearchParams(window.location.search);
        const sid = params.get("sid");
        if (sid) {
          window.school.setStudentId(sid);
          const url = new URL(window.location);
          url.searchParams.delete("sid");
          window.history.replaceState({}, "", url.pathname + url.search);
          window.school.updateEnrolledIndicator();
          window.school.fetchProgress().then(() => {
            window.school.updateAllCheckmarks();
          });
        } else if (window.school.getStudentId()) {
          // Show cached state immediately, then refresh from server
          window.school.updateEnrolledIndicator();
          window.school.updateAllCheckmarks();
          window.school.fetchProgress().then(() => {
            window.school.updateAllCheckmarks();
          });
        }

        // Poll for progress updates every second so the UI reflects changes
        // made by agents (e.g. marking a lesson complete) without a page reload.
        if (window.school.getStudentId()) {
          setInterval(function() {
            window.school.fetchProgress().then(function() {
              window.school.updateAllCheckmarks();
            });
          }, 1000);
        }
      })();
    </script>
  </body>
</html>
