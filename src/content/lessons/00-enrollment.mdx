---
title: "Enrollment"
slug: enrollment
description: "Get your student ID, pick a theme color, and start tracking your progress."
order: 0
acceptanceCriteria: "Student has a student ID stored in localStorage, a theme color preference stored in localStorage, and a corresponding progress record exists on the server."
agentInstructions: "This lesson is completed automatically via the website. Direct the student to visit https://opencode.school/lessons/enrollment, click the 'Generate my student ID' button, choose a color, and let the website finish enrollment. Do not attempt to complete this lesson programmatically."
---

<div id="enrollment-area">

<div id="not-enrolled">

Click the button below to generate your student ID — a unique identifier that tracks your progress through the course.

<button type="button" id="enroll-button" class="theme-button mt-2 cursor-pointer rounded-lg px-6 py-3 font-mono text-sm disabled:opacity-50 disabled:cursor-not-allowed">Generate my student ID</button>

<p id="color-picker-copy" class="mt-4 text-sm text-gray-600 dark:text-gray-400 hidden">Choose a color for your student ID card and site theme.</p>

<div id="color-picker" class="mt-4 hidden grid grid-cols-4 gap-3 sm:grid-cols-8 not-prose">
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="blue" aria-label="Blue" title="Blue"><span class="block h-full w-full rounded-full" style="background:#2563eb"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="indigo" aria-label="Indigo" title="Indigo"><span class="block h-full w-full rounded-full" style="background:#4f46e5"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="violet" aria-label="Violet" title="Violet"><span class="block h-full w-full rounded-full" style="background:#7c3aed"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="purple" aria-label="Purple" title="Purple"><span class="block h-full w-full rounded-full" style="background:#9333ea"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="fuchsia" aria-label="Fuchsia" title="Fuchsia"><span class="block h-full w-full rounded-full" style="background:#c026d3"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="rose" aria-label="Rose" title="Rose"><span class="block h-full w-full rounded-full" style="background:#e11d48"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="red" aria-label="Red" title="Red"><span class="block h-full w-full rounded-full" style="background:#dc2626"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="orange" aria-label="Orange" title="Orange"><span class="block h-full w-full rounded-full" style="background:#ea580c"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="amber" aria-label="Amber" title="Amber"><span class="block h-full w-full rounded-full" style="background:#b45309"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="lime" aria-label="Lime" title="Lime"><span class="block h-full w-full rounded-full" style="background:#4d7c0f"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="green" aria-label="Green" title="Green"><span class="block h-full w-full rounded-full" style="background:#15803d"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="emerald" aria-label="Emerald" title="Emerald"><span class="block h-full w-full rounded-full" style="background:#059669"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="teal" aria-label="Teal" title="Teal"><span class="block h-full w-full rounded-full" style="background:#0f766e"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="cyan" aria-label="Cyan" title="Cyan"><span class="block h-full w-full rounded-full" style="background:#0e7490"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="sky" aria-label="Sky" title="Sky"><span class="block h-full w-full rounded-full" style="background:#0284c7"></span></button>
  <button type="button" class="enrollment-color-option h-12 w-12 rounded-full border border-gray-200 bg-white p-1.5 shadow-sm dark:border-gray-700 dark:bg-gray-900" data-theme-color="slate" aria-label="Slate" title="Slate"><span class="block h-full w-full rounded-full" style="background:#475569"></span></button>
</div>

<p id="enroll-error" class="mt-2 text-red-500 text-sm hidden">Something went wrong. Please try again.</p>

</div>

<div id="enrolled" class="hidden">

<div id="student-id-card" class="my-8 rounded-2xl overflow-hidden shadow-sm border border-gray-200 dark:border-gray-700/50">
  <div class="theme-card-band px-5 py-2">
    <p class="text-lg font-mono uppercase tracking-[0.25em] text-center">HELLO! My OpenCode Student ID is</p>
  </div>
  <div class="bg-gray-50 dark:bg-gray-900/80 px-6 py-4">
    <p id="student-id-display" class="text-3xl md:text-4xl font-mono font-bold text-gray-800 dark:text-gray-100 tracking-tight text-center"></p>
  </div>
</div>

<p id="enrolled-message" class="text-lg"><span class="font-bold">You're enrolled!</span> We'll use this to track your progress as you complete each lesson. Your **progress is stored locally in your web browser**. OpenCode School does not require an account, and does not track any personally identifying information.</p>

</div>

</div>

<script>{`
document.addEventListener("DOMContentLoaded", function() {
  var enrollBtn = document.getElementById("enroll-button");
  var colorPickerCopy = document.getElementById("color-picker-copy");
  var colorPicker = document.getElementById("color-picker");
  var enrollError = document.getElementById("enroll-error");
  var notEnrolled = document.getElementById("not-enrolled");
  var enrolled = document.getElementById("enrolled");
  var display = document.getElementById("student-id-display");
  var colorOptions = Array.prototype.slice.call(document.querySelectorAll(".enrollment-color-option[data-theme-color]"));

  if (!enrollBtn || !window.school) return;

  var selectedTheme = window.school.getThemeColor();
  var isSubmitting = false;

  var chars = "abcdefghijklmnopqrstuvwxyz";
  var digits = "0123456789";

  function randomChar(real) {
    if (real === "-") return "-";
    if (real >= "0" && real <= "9") return digits[Math.floor(Math.random() * digits.length)];
    return chars[Math.floor(Math.random() * chars.length)];
  }

  function scramble(target) {
    var result = "";
    for (var i = 0; i < target.length; i++) {
      result += randomChar(target[i]);
    }
    return result;
  }

  function fireConfetti() {
    var duration = 2000;
    var end = Date.now() + duration;
    var palettes = window.school.getThemePalettes();
    var palette = palettes[window.school.getThemeColor()] || palettes.blue;
    var colors = [palette.solid, palette.solidHover, palette.linkDark, palette.softText, palette.softTextDark];

    (function frame() {
      window.confetti({
        particleCount: 6,
        angle: 90,
        spread: 80,
        origin: { x: 0.5, y: 0 },
        colors: colors
      });
      if (Date.now() < end) requestAnimationFrame(frame);
    })();
  }

  function cipherReveal(target, callback) {
    var resolved = 0;
    var current = scramble(target);
    display.textContent = current;

    var scrambleInterval = setInterval(function() {
      var next = "";
      for (var i = 0; i < target.length; i++) {
        if (i < resolved) {
          next += target[i];
        } else if (target[i] === "-") {
          next += "-";
        } else {
          next += randomChar(target[i]);
        }
      }
      current = next;
      display.textContent = current;
    }, 30);

    setTimeout(function() {
      var resolveInterval = setInterval(function() {
        if (resolved >= target.length) {
          clearInterval(scrambleInterval);
          clearInterval(resolveInterval);
          display.textContent = target;
          if (callback) callback();
          return;
        }
        if (target[resolved] === "-") {
          resolved++;
        }
        resolved++;
      }, 140);
    }, 600);
  }

  function showEnrolled(studentId, animate) {
    notEnrolled.classList.add("hidden");
    enrolled.classList.remove("hidden");

    // Hide the lesson description and header prose since we show our own enrolled message
    var article = document.querySelector("article.prose");
    if (article) {
      // Hide the intro paragraph that's part of the prose wrapper
      var proseChildren = article.querySelectorAll(":scope > p");
      proseChildren.forEach(function(p) {
        if (p.closest("#enrollment-area")) return;
        p.classList.add("hidden");
      });
    }

    if (animate) {
      cipherReveal(studentId, function() {
        fireConfetti();
        // Auto-complete the enrollment lesson
        window.school.markComplete("enrollment").then(function() {
          window.school.updateAllCheckmarks();
          // Update the enrolled-as indicator in the layout
          if (typeof window.school.updateEnrolledIndicator === "function") {
            window.school.updateEnrolledIndicator();
          }
        });
      });
    } else {
      display.textContent = studentId;
    }
  }

  function setSelectedTheme(themeName) {
    selectedTheme = window.school.setThemeColor(themeName);
    colorOptions.forEach(function(option) {
      option.classList.toggle("is-selected", option.getAttribute("data-theme-color") === selectedTheme);
    });
  }

  function setPickerState(message, disabled) {
    if (colorPickerCopy && message) colorPickerCopy.textContent = message;
    colorOptions.forEach(function(option) {
      option.disabled = disabled;
      option.classList.toggle("opacity-60", disabled);
      option.classList.toggle("cursor-not-allowed", disabled);
    });
  }

  function revealColorPicker() {
    enrollBtn.classList.add("hidden");
    if (colorPickerCopy) colorPickerCopy.classList.remove("hidden");
    if (colorPicker) colorPicker.classList.remove("hidden");
    setSelectedTheme(selectedTheme);
  }

  var existingId = window.school.getStudentId();
  if (existingId) {
    showEnrolled(existingId, false);
  } else {
    setSelectedTheme(selectedTheme);
  }

  enrollBtn.addEventListener("click", function() {
    revealColorPicker();
  });

  colorOptions.forEach(function(option) {
    option.addEventListener("click", async function() {
      if (isSubmitting) return;

      isSubmitting = true;
      enrollError.classList.add("hidden");
      setSelectedTheme(option.getAttribute("data-theme-color"));
      setPickerState("Generating your student ID...", true);

      try {
        var data = await window.school.enroll();
        showEnrolled(data.studentId, true);
      } catch (e) {
        enrollError.classList.remove("hidden");
        setPickerState("Choose a color for your student ID card and site theme.", false);
        isSubmitting = false;
      }
    });
  });
});
`}</script>

<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.9.3/dist/confetti.browser.min.js"></script>
